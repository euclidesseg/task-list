import { createApp } from 'vue'
import App from './App.vue' // importa nuestra appa App.vue

// Punto de entrada principal de la aplicacion
createApp(App).mount('#app') // Monta la aplicacion en el div con id app

